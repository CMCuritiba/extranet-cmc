-c constraints.txt
-e src/extranetcmcuritibaprlegbr[test]

zope.testrunner

# Add required add-ons
# (Ideally add them in setup.py for extranetcmcuritibaprlegbr)
# collective.easyform
pas.plugins.oidc
pas.plugins.memberpropertytogroup